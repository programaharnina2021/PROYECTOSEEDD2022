package ejemplos;

import java.util.Arrays;
import java.util.List;

public class Prueba {

	/**
	 * encuentra multiplos del divisor entre el min y el max
	 * @param min valor minimo del rango
	 * @param max valor maximo del rango
	 * @param divisor 
	 * @return lista de los multiplos del divisor
	 */
 public List encontrarMultiplos(int min,int max,int divisor){
	 //TODO
		int[] resultado = { 2, 4, 6, 8 };
		return Arrays.asList(resultado);
 }
}
